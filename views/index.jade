script
  $(function() {
    $('.activities').each(function() {
      var $element = $(this)
      $element.load('/activities', {
        username: $element.data('username')
      })
    })
  })

h1 PivotalTacker TimeTravel

#project-information
  span.icon-info-sign
  span
    = "&nbsp;"
    = "Project: " + config.projectId
    = ", Activities: " + activityCount

br
br

#users
  - config.usernames.split(',').forEach(function(username) {
    .well.activities(data-username=username.trim(), style="padding: 8px 0;")
    hr
  - })
